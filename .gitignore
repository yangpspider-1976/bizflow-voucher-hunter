node_modules
.next
.next-dev
dist
coverage
playwright-report
test-results
.env
.env.*
!.env.example
*.log
tsconfig.tsbuildinfo
.DS_Store
data/*.db
data/*.db-shm
data/*.db-wal

.vercel

# Downloaded EAS build artifacts. Also keeps them out of the EAS upload: with no
# .easignore, `eas build` falls back to this file, and 221MB of preview APKs was
# being shipped to the build servers on every run.
builds/

# JetBrains IDE workspace settings.
.idea/

# Claude Code agent tooling: skills, scaffolding and their lockfile. Local
# working files rather than app or server code, and EAS refuses a dirty tree.
.agents/
.claude/skills/
skills-lock.json
